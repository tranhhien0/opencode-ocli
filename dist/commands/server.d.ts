/**
 * OCX - OpenCode eXtension CLI
 * Server commands: serve, web, attach
 */
import { Command } from 'commander';
declare const server: Command;
export { server };
declare const serve: Command;
declare const web: Command;
declare const attach: Command;
export { serve, web, attach };
